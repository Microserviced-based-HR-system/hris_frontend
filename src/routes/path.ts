interface Paths {
   [key: string]: string;
}

export default {
   HOME: '/',
   LOGIN: '/login',
   REGISTER: '/register',
   NOT_FOUND: '*',
   DEPARTMENT: '/departments',
   CANDIDATE: '/candidate',
} as Paths;
